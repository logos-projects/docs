# AI 可稳定理解的软件规格体系
author：jonathan

## 1 简述

基于 AI Agent 驱动的软件工程体系，无论应用于何种场景，本质上都需要围绕多个核心能力维度展开。与传统软件工程相比，AI Agent 的引入不仅改变了开发方式，也正在重塑技术人员、项目管理者以及组织协作的整体形态。

- 需要具备AI Agent 工具的使用能力
如同传统软件开发需要掌握编程语言与开发框架一样，AI Agent 时代同样需要熟练掌握各类 Agent 工具、模型能力以及协同方式。不仅要能够使用，更需要理解其边界、成本、效率与适用场景，从而形成工程化、高效率的协同能力。**这是技术人员最基础的能力要求**。
- 需要具备软件工程 SOP 与 AI 协同流程能力
在 AI Agent 深度参与研发之后，传统的软件工程流程正在发生明显变化，包括需求分析、任务拆解、代码生成、测试验证以及文档维护等环节，都需要重新设计更加适合 AI 协同的新型 SOP 与工作机制。这对项目管理、技术管理以及团队协同提出了更高要求。
- 行业知识与工程经验的重要性不仅没有降低，反而更加突出
AI Agent 可以辅助生成代码与文档，但无法替代对行业逻辑、业务规则、风险控制以及真实场景的理解。尤其在金融、安全、区块链、医疗等复杂领域，长期积累的行业经验与工程经验，仍然是决定系统质量与业务可行性的核心因素。
- 逻辑化与结构化文档能力，正在成为 AI Agent 时代极为重要的基础能力
AI Agent 的运行高度依赖上下文质量，而高质量的上下文，本质上来源于结构清晰、逻辑严谨、边界明确的文档体系。因此，需求文档、架构文档、流程文档、规则文档以及知识库建设，将不再只是辅助性工作，而会逐渐成为 AI 协同开发中的核心基础设施。
- 任务拆解与上下文设计能力也正在变得越来越重要
AI Agent 更适合处理边界清晰、目标明确、可验证的任务，因此如何将复杂问题拆解为多个可执行、可验证、可迭代的子任务，将成为未来软件工程中的关键能力。优秀工程师之间的重要差异，也将逐渐从“编码能力”转向“问题抽象能力”与“上下文组织能力”。
- 验证、审计与结果校验能力将成为 AI 工程体系中的核心环节
AI Agent 可以快速生成结果，但并不意味着结果天然正确。在金融、安全以及高可靠性系统中，AI 输出必须经过严格验证，包括自动化测试、静态分析、形式化验证、仿真验证以及多 Agent 交叉校验等机制。因此，“可验证”“可审计”“可回溯”“可复现”会逐渐成为 AI 软件工程的重要基础原则。
- 在组织协作层面，人机协同与角色分工模式也正在发生改变
未来的软件工程将不再是单纯“人编写代码”的模式，而更像是“人负责目标、约束与决策，AI 负责生成、执行与辅助分析”的协同体系。开发人员、架构师、测试人员、产品经理以及 AI Agent 之间的职责边界，会逐渐从“执行型分工”转向“协同型分工”。
- 知识库与长期记忆体系建设能力，将逐渐成为 AI Agent 软件工程中的核心资产
未来组织最重要的资产，可能不再只是代码仓库，而是包括 Prompt 仓库、SOP 仓库、架构决策记录、领域知识库、验证规则库以及 Agent Workflow 在内的整体知识体系。AI Agent 的能力上限，很大程度上取决于组织知识的积累质量与结构化程度。

总体而言，AI Agent 正在推动软件工程从“代码驱动”逐渐走向“上下文驱动”“知识驱动”以及“验证驱动”的新阶段。技术人员关注的重点、团队协作方式以及组织能力结构，都正在发生深刻变化。

本文档基于《可验证金融行为系统》的实践工作总结而来，目前仍存在诸多不足，后续将持续迭代、补充与优化。

## 2 AI 驱动开发的本质

对于 AI Agent 驱动开发来说，AI Agent不是工程师，不会主动形成“统一产品理解”， 并不会“自动理解你的系统应该长什么样”。

因此如果文档不完整，AI 会：

- 自行脑补
- 漂移
- 模块不一致
- 状态不一致
- UI 风格漂移
- API 不一致
- 命名漂移
- 事件漂移

所以，AI 项目的核心是“上下文控制（Context Control）”，而相关的文档就是相应的上下文。

AI Agent 最重要的是“可确定性”，它只能：

- 根据明确上下文生成
- 根据结构化约束推导
- 根据确定规则实现

最怕的是：“业务抽象正确，但交互不明确”

- 模糊描述
- 缺状态定义
- 缺页面关系
- 缺展示定义


## 3 AI Software Spec System

AI 驱动开发需要哪些文档？完整文档体系：

| 层级 | 文档           | 作用      |
| -- | ------------ | ------- |
| L0 | Vision       | 为什么存在   |
| L1 | Product      | 产品是什么   |
| L2 | UX           | 用户怎么使用  |
| L3 | Domain       | 业务规则    |
| L4 | Architecture | 系统怎么组成  |
| L5 | Engineering  | 工程如何实现  |
| L6 | Runtime      | 系统如何运行  |
| L7 | AI Guidance  | AI 如何生成 |

文档详细说明：

- 理论、架构、领域方面的描述
  
- vision.md（世界观文档）  
  这是“系统灵魂”，作用是告诉 AI：
  ```
  系统为什么存在
  核心理念是什么
  哪些东西不能违背
  ```
  这是 AI 的“世界观”。

  - 系统目标  
    例如：
    ```
    构建可验证金融基础设施
    ```
  - 核心原则  
    例如：
    ```
    所有金融行为必须可回放
    所有行为必须带RCS
    所有账本必须可验证一致
    ```
  - 核心理论  
    例如：
    - LMM
    - PAL
    - CAV
    - RSG
    - SEP
  - 不可违背约束（极重要）  
    例如：
    ```
    禁止模块直接绕过EventBus
    禁止无RCS事件
    禁止账本单写
    ```
  **AI 非常依赖**
  否则它会偷懒
  
  AI 非常适合“遵守”，但不适合“定义”

    | 内容   | AI适合度 |
    | ---- | ----- |
    | 遵守架构 | 高     |
    | 理解理论 | 高     |
    | 创造理论 | 低     |
  
- prd.md（产品定义文档）  
  这是“产品边界”, 作用是告诉 AI：
  ```
  系统到底有哪些功能
  ```
  这是 AI 的“产品理解”。

  - 用户角色  
    例如：
    | Role         | Description |
    | ------------ | ----------- |
    | User         | 普通用户        |
    | BankOperator | 银行操作员       |
    | Auditor      | 审计员         |
    | Regulator    | 监管          |

  - 功能模块  
    例如：
    ```
    Issue
    Redeem
    Replay
    Audit
    RegTech
    ```
  - 每个功能做什么  
    例如：
    Issue, 用户将 EUR 转换为 eEUR
  - 功能边界  
    例如：
    ```
    Demo不包含真实银行
    Demo不包含真实BTC
    ```
  **AI 非常依赖**
  否则会无限扩展
  
  AI 适合做吗？
  
  | 内容     | AI适合度 |
  | ------ | ----- |
  | 生成功能代码 | 极高    |
  | 推导产品边界 | 低     |
  
  产品边界必须人定义。
- ux-flow.md（用户流程文档）  
  这是“行为上下文”, 作用告诉 AI：
  ```
  用户如何一步步操作
  ```

  - 用户路径  
    例如：
    ```
    登录
    → Dashboard
    → Issue
    → 输入金额
    → 确认
    → 签名
    → Processing
    → Replay
    ```
  - 异常路径
    例如：
    ```
    KYC失败
    → 显示 blocked
    ```
  - 页面跳转
    例如：
    ```
    Issue成功
    → transaction/[rcs]
    ```
  **AI 非常依赖**
  因为AI 不知道：
  - 页面之间关系
  - 按钮行为
  - wizard
  - modal
  
  AI 适合做吗？

  | 内容     | AI适合度 |
  | ------ | ----- |
  | 生成页面   | 极高    |
  | 设计用户流程 | 中低    |
  
- ui-spec.md（UI规范）  
  这是“视觉上下文”, 作用告诉 AI：
  ```
  页面长什么样
  ```
  这是 AI 的“前端生成依据”。

  - 页面列表  
    例如：
    ```
    /dashboard
    /issue
    /replay/[rcs]
    ```
  - 页面布局  
    例如：
    ```
    左侧：Timeline
    右侧：Hash/Card
    ```
  - UI风格
    例如：
    ```
    金融科技风
    深色
    Terminal + Dashboard
    ```
  - 组件定义
    例如：
    ```
    ReplayTimeline
    HashCard
    ResponsibilityChain
    ```
  **AI 非常依赖**
  否则：UI 会完全漂移。
  
  AI 适合做吗？

  | 内容     | AI适合度 |
  | ------ | ----- |
  | 生成组件   | 极高    |
  | 统一视觉体系 | 中     |

- domain-rules.md（领域规则文档）  
  这是“业务真相”, 作用告诉 AI：
  ```
  业务规则是什么
  ```
  这是 AI 的“业务规则”。

  - Iusse规则
    例如：
    ```
    KYC必须PASS
    Jurisdiction必须允许
    ```
  - 法域规则
  - 风控规则
  - 状态规则
    例如：
    ```
    EXECUTED后不可回滚
    ```
  - CAV一致性规则  
    例如：
    ```
    CBS.amount
    =
    Iusse.amount
    =
    PAL.amount
    ```

  **AI 不适合创造**
  因为AI 容易逻辑漂移。这里必须人定义。
  
- architecture.md（架构文档）  
  这是“工程结构”, 作用告诉 AI：
  ```
  系统如何组成
  ```
  这是 AI 的“工程架构”。

  - **模块**
  - **EventBus**
  - **状态机**
  - **数据流**
  - **分层**
  - **RCS传播**
 
  AI 适合吗？

  极高。这是 AI 最擅长遵守的。

- api-contract.md（接口契约）  
  这是“协作协议”, 作用告诉 AI：
  ```
  前后端如何协作
  ```
  这是 AI 的“协作协议”。

  - **request**
  - **response**
  - **error code**
  - **websocket event**
  - **trace id**
  - **idempotency**
 
  **AI 非常依赖**
  否则：前后端会乱。

- database-schema.md  
  这是“数据真相”，作用告诉 AI：
  ```
  数据如何存
  ```
  
  - **表**
  - **关系**
  - **状态字段**
  - **JSON结构**
  - **Event结构**
 
  **AI 非常适合**
  AI 生成 Prisma 极强。
  
- event-model.md（极重要）  
  这是“系统血液”，作用告诉 AI：
  ```
  系统如何通信
  ```

  - **Event结构**
  - **Topic**
  - **Producer**
  - **Consumer**
  - **顺序性**
  - **重试**
  - **幂等**
 
  **AI 非常依赖**
  否则：Event 会漂移。

- replay-spec.md（超级重要）  
  这是“系统核心灵魂”，作用告诉 AI：
  ```
  Replay到底是什么
  ```

  - **Replay JSON结构**
  - **Timeline结构**
  - **PAL展示**
  - **Hash结构**
  - **Replay UI**
  - **Replay API**
 
  **AI 不会自动推导 Replay**
  必须人定义。
  
- engineering-guide.md  
  这是“代码生成规范”，作用告诉 AI：
  ```
  代码怎么写
  ```

  - **Monorepo结构**
  - **命名规范**
  - **DTO规范**
  - **文件夹结构**
  - **Service Pattern**
  - **Event Naming**
  - **TypeScript规范**
  - **状态机规范**
 
  **AI 极度依赖**
  否则代码会越来越乱。

- ai-agent-guide.md（关键）  
  这是“AI操作手册”，作用告诉 AI：
  ```
  如何生成代码
  ```

  - **哪些不能改**  
    例如：
    ```
    RCS字段不可删除
    ```
  - **哪些必须生成**
    例如：
    ```
    所有event必须带trace_id
    ```
  - **哪些禁止**
    例如：
    ```
    禁止直接跨模块调用
    ```
  - **优先级**
    例如：
    ```
    Replay优先于优化
    ```

  **这是未来 AI 工程的关键**

## 4 哪些适合 AI 做

**AI 非常适合的**
| 模块            | AI适合度 |
| ------------- | ----- |
| CRUD          | 极高    |
| Dashboard     | 极高    |
| Prisma Schema | 极高    |
| API           | 极高    |
| DTO           | 极高    |
| Replay UI     | 极高    |
| EventBus      | 高     |
| 状态机           | 高     |
| Tailwind页面    | 极高    |
| Admin后台       | 极高    |
| Mock数据        | 极高    |


**AI 中等适合**
| 模块        | 原因   |
| --------- | ---- |
| 状态一致性     | 需要规则 |
| Event顺序   | 需要约束 |
| WebSocket | 易漂移  |
| Replay聚合  | 需要规范 |

**AI 不适合**
| 模块     | 原因     |
| ------ | ------ |
| 金融责任体系 | 需要业务定义 |
| 法律边界   | AI不懂   |
| CAV规则  | 需要严格定义 |
| 风控策略   | 需要经验   |
| 审计规则   | 需要金融背景 |
| PAL语义  | AI会漂移  |

## 5 总结

真正的 AI 开发模式 是 “Spec-Driven AI Development”：
```
人：
定义规则
定义边界
定义架构
定义语义

AI：
生成实现
生成页面
生成接口
生成状态机
生成样板代码
```
